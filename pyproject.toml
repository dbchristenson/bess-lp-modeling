[project]
name = "assignment3"
version = "0.1.0"
description = "BESS + Demand Response optimization model for a 10 MW Dublin data center (ISEN 495)"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.9",
    "numpy>=2.0",
    "openpyxl>=3.1",
    "pyomo>=6.7",
    "highspy>=1.7",
    "python-docx>=1.2.0",
]

[project.scripts]
build-model = "build_model:main"
build-figures = "build_figures:main"
